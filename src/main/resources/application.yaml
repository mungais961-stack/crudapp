spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cloud_vendor2?useSSL=false
    username: root
    password: Simone@001

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
